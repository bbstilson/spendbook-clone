import React from 'react';
import Loading from 'react-simple-loading';

export default () => (
  <div style={{ position: 'absolute', width: '100%', height: '100%' }}>
    <Loading stroke="4px" color="#fff" />
  </div>
);
